### 生命周期
- `clean`： 清理工作
	- `clean`：除上一次构建生成的文件
- `default`： 核心工作，如：编译、测试、打包、安装、部署
	- `compile`： 译项目源代码
	- `test`：用合适的单元测试框架运行测试（junit）
	- `package`：将编译后的文件打包，如：jar、war等
	- `install`：装项目到本地仓库
- `site` ：生成报告、发布站点
`clean`->`install`
> 在**同一套**生命周期中，当运行后面的阶段时前面的阶段都会运行。